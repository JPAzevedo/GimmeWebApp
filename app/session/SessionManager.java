package session;

import entities.RemoteSession;

public class SessionManager{

    private RemoteSession sessionObj;

    public SessionManager(RemoteSession sessionObj){
        this.sessionObj = sessionObj;
    }

    // Save Hashed Token

    // Get Hashed Token

    // Implement Cache to get the Hashed Token + Expiration Date

    // Check Token
}